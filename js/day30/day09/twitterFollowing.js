'use strict';

import React, { Component } from 'react';
import {
	View
} from 'react-native';


const TwitterFollowing = (props) => {
  props: Props;

	const { data } = props;
	return (
		<View style={styles.containerStyle}>
		</View>
	);
};

export default TwitterFollowing;

const styles = {
	container: {
		flex: 1,
	},
};


